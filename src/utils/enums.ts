export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other',
}

export enum Visibility {
  PUBLIC = 'public',
  PRIVATE = 'private',
  FOLLOWER_ONLY = 'follower_only'
}

export enum ReactionType {
  LIKE = 'like',
  SAD = 'sad',
  ANGRY = 'angry',
  WOW = 'wow',
  CARE = 'care',
}
